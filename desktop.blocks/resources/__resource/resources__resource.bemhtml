block('resources').elem('resource').content()(function() {
    var ctx = this.ctx;
    return [
        {
            block: 'link',
            mods: { arrow: true },
            url: ctx.url,
            content: applyNext()
        },
        ctx.type,
        ' &middot; ',
        ctx.source
    ]
})
